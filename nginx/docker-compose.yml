# docker-compose.yml
version: '3.8'

services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nginx-container
    ports:
      - "8080:8080"
      - "443:443"
    volumes:
      - ./certs:/etc/nginx/ssl
